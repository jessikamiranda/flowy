# Environment variables
#
# When initializing a new project, this file is copied to `.env.local`.
# Add project-specific environment variables below.
#
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser.
# Never store secrets in NEXT_PUBLIC_ variables.

# Example:
# NEXT_PUBLIC_API_URL=