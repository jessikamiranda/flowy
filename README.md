# Flowy

A modern client and project operations platform for teams

## Getting Started

Install the dependencies:

```bash
npm install
```

Start the development server:

```bash
npm run dev
```

Run the project quality checks:

```bash
npm run check
```

Run the complete validation pipeline:

```bash
npm run validate
```
